import { Box } from "@mui/material"
import { styled } from "@mui/system"

const StyledDataGridToolbarBox = styled(Box)({
    width: "100%",
    marginBottom: "8px",
    display: "flex",
    justifyContent: "space-between",
    alignItems: "self-end",
    flexWrap: "wrap",
    gap: 1,
})

export default StyledDataGridToolbarBox
